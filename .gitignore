.build/
.build-*/
dist/
.DS_Store
*.xcodeproj
*.xcworkspace
.swiftpm/
.claude/

# Apple signing artifacts — personal to each developer's team, never committed.
# Bring your own: see the signing section of build_app.sh (PROVISION_PROFILE env var).
*.provisionprofile
*.mobileprovision
*.p12
*.cer

# Internal working docs — agent notes and planning logs, kept local only.
CLAUDE.md
BACKLOG.md
AGENT_LAYER_PLAN.md
NOTES_APP_PLAN.md
