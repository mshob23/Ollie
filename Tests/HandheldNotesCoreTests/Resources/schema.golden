# Golden schema fingerprint hash (F5 release gate). SHA256 of the deterministic,
# sorted SwiftData Schema(NotesDataStore.modelTypes) fingerprint — every synced
# @Model (NoteEntity + the agent-layer TagEntity / MemoryEntity / ViewRevisionEntity
# / InstructionsEntity). Regenerate when any of them changes — SchemaGoldenTests
# prints the new hash + fingerprint on failure — AND deploy the schema to CloudKit
# Production before release. See
# Tests/HandheldNotesCoreTests/SchemaGoldenTests.swift and RELEASE.md.
64eb442ba072e725609209619179fddf9edb2229627a3aae73fdd9a59797fa58
